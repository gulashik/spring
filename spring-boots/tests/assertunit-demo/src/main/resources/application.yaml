spring:
  application:
    name: assertj-demo
  datasource:
    url: jdbc:h2:mem:testdb # todo url ??? ??????? H2
  jpa:
    generate-ddl: true # todo ???????? ????? ??? ???????
    hibernate:
      ddl-auto: create # todo ???? ?? embeded ?? ????? ??? ??????? validate
        # none - Disable DDL handling.
        # validate - Validate the schema, make no changes to the database.
        # update - Update the schema if necessary.
        # create - Create the schema and destroy previous data.
      # create-drop - Create and then destroy the schema at the end of the session
    properties:
      hibernate:
        format_sql: true
    show-sql: true # todo ddl ? ?????
  sql:
    init:
      mode: never # todo ????????? ?????????? schema.sql ? data.sql ??? ????????? ??????????